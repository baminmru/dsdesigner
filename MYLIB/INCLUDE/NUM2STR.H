/****************************************************
 *      NUM2STR.H                                   *
 *  Copyright (C) Михаил М. Баранов                 *
 *  Модификация без предварительного согласия автора*
 *  будет рассматривается как нарушение авторского  *
 *  и имущественного права                          *
 *  создан: 21 Сентября 1995, Четверг               *
 ****************************************************/
#ifndef NUM2STR
#define NUM2STR
// 25.08.93 Хилов П.Е.

// Файл объявления функций для преобразования
//   чисел в прописную форму
//

// Мнемонические константы для задания рода единиц измерения
// Метр - MALE, тонна - FEMALE

enum GENDER { MALE,
	      FEMALE };

// -----------------------------------------------------------------
// основная функция
// numb - число для преобразования
// dest - строка, в которую помещается результат (место должно быть
//                                       отведено до вызова функции)
// GENDER - род (см. выше)
// Возвращаемое значение

int parseNumber( long double numb, char *dest, int gender );

// -----------------------------------------------------------------
// Функция для приведения единицы измерения в соотв. форму
// numb - преобразуемое число
// root - единственное число именительный падеж единицы измерения
//        (пока обрабатывает единицы мужского рода без мягкого знака
//         в конце. Рубль - исключение )
// dest - строка, в которую помещается результат (место должно быть
//                                       отведено до вызова функции)

int male2string( long double numb , char *root, char *dest );

// Вспомогательная функция
// переводит строку s в верхний регистр и помещает в строку d,
// корректно обрабатывая русские буквы

//int toupprus( const char *s, char *d );

//int divideString( char *str, int part );
#endif
